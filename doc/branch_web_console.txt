REQUIREMENTS

DONE - zero install (probably python bottle)

- surface all the setup_tool features in a web context

  - monitoring interface (basically registry list)
    perhaps a panel that shows all registered devices
    picture of the raw energenie device
    user assigned picture for what is plugged into it?
    control status (demands)  show switch status (demand only if rx only device)
    - click on a device switch to turn it on and off
    monitor status (readings) show all readings as they arrive

  - other buttons
    - legacy learn mode start/stop
    - mihome discovery start/stop
    - rename device
    - delete device

    - logging mode
    (log all data to a scrollable window and file)
    (download/clear the data file)
